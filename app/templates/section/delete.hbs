{{page-title "Delete"}}
<div class="ui segment">
  <form class="ui form">
    <h4 class="ui dividing header">Supprimer cette section : {{@model.name}}</h4>
    <LinkTo class="ui button" @route="section">Annuler</LinkTo>
    <button class="ui button"{{action 'delete' @model}}>Enregistrer</button>
  </form>
</div>
{{outlet}}